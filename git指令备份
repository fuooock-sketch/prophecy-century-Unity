git status
git branch --show-current
git add .
git commit -m "XXXXX"
git pull --rebase origin main
git push origin main

先直接这样排查

按顺序复制执行这几条：

Test-NetConnection github.com -Port 443
git remote -v
git config --global --get http.proxy
git config --global --get https.proxy

你重点看两件事：

TcpTestSucceeded 是不是 True
Git 有没有配置代理

如果代理有值，先清掉再试

如果上面两条 proxy 命令有输出，比如 127.0.0.1:7890 这种，那就先执行：

git config --global --unset http.proxy
git config --global --unset https.proxy

然后再执行：

git config --global http.version HTTP/1.1
git config --global http.sslBackend schannel
git push origin main


先执行这两条，再推一次：

git config --global http.version HTTP/1.1
git config --global http.sslBackend schannel
git push origin main



git status
git branch --show-current
git switch -c ui-polish
git branch --show-current
git status

# 预言世纪 Git 直接操作文档

适用工程目录：

`C:\projectZhongxu\prophecy_century\prophecy-century-ChatGpt\work_v1_final`

适用环境：

- Windows
- PowerShell
- GitHub 远程仓库
- 当前主分支：`main`

---

# 1. 先记住 Git 最核心的逻辑

你现在只需要这样理解：

- `main` = 稳定主线
- 其他分支 = 专门干某件事的施工线
- `git status` = 看现在什么状态
- `git add .` = 把改动放进待提交区
- `git commit -m "说明"` = 生成一次本地版本记录
- `git pull` = 拉远程最新代码到本地
- `git push` = 把本地提交传到远程 GitHub

一句话：

> 先看状态 → 再提交 → 再同步 → 再推送

---

# 2. 最常用的 8 个命令

## 看当前状态

```powershell
git status